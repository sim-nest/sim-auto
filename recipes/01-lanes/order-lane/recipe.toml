id = "auto-lane-order"
title = "Modeled order lane"
codec = "rust"
setup = "src/main.rs"
purpose = "README.md"
order = 60
tags = ["auto", "modeled", "lane", "order"]
requires = ["sim-lib-auto-order"]
