id = "auto-modeled-work-order"
title = "Modeled work order"
codec = "rust"
setup = "src/main.rs"
purpose = "README.md"
order = 10
tags = ["auto", "modeled", "work-order", "conformance"]
requires = ["sim-lib-auto-order"]
