[package]
name = "auto-site-ista"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
description = "Runnable modeled ISTA site recipe for sim-auto."
readme = "README.md"
publish = false

[dependencies]
auto-recipe-support = { path = "../../auto-recipe-support" }
sim-kernel = "0.1.0"

[lints]
workspace = true
