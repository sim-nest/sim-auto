# Modeled Service Lane

Checks the modeled service lane and its denied coding actions. The recipe keeps
service writes visible in the work-order ledger while proving the public parent
grant set does not silently delegate service-write authority.
