id = "auto-site-autotuner"
title = "Modeled Autotuner site"
codec = "rust"
setup = "src/main.rs"
purpose = "README.md"
order = 190
tags = ["auto", "modeled", "site", "autotuner"]
requires = ["sim-lib-auto-order"]
