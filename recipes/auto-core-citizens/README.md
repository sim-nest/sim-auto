# Auto Core Citizens Recipe

This runnable recipe registers the automotive core citizens, runs the explicit
read-construct conformance sweep, installs the loadable automotive core library,
and prints the exported capability symbol.

Run it from the repository root:

```bash
cargo run --manifest-path recipes/auto-core-citizens/Cargo.toml
```
